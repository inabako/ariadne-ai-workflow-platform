# test_github_knowledge_maintenance.py

??????? `runtime/tests/test_github_knowledge_maintenance.py` ?pytest node id??UT?????

| ?? | ? |
| --- | ---: |
| cases | 20 |

## ?????

#### RT-UT-CASE-130

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_build_parser_parses_every_subcommand
```

- 確認内容: pytest case `build parser parses every subcommand` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:154`
  - fixture/arg: なし
  - parameter: names=なし, case=なし
  - inline input: `calls`
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-131

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_repository_name_and_default_work_id_variants
```

- 確認内容: pytest case `repository name and default work id variants` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:167`
  - fixture/arg: なし
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-132

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_init_work_rejects_existing_without_reuse_and_script_load
```

- 確認内容: pytest case `init work rejects existing without reuse and script load` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:182`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-133

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_gate_and_tool_selection_proposal_mode_do_not_require_human_check
```

- 確認内容: pytest case `gate and tool selection proposal mode do not require human check` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:204`
  - fixture/arg: なし
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-134

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_register_github_knowledge_contexts_skips_missing_files_and_registers_existing
```

- 確認内容: pytest case `register github knowledge contexts skips missing files and registers existing` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:219`
  - fixture/arg: `monkeypatch` (environment / function monkeypatch), `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: `calls`
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-135

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_markdown_helpers_render_empty_values_booleans_lists_and_titles
```

- 確認内容: pytest case `markdown helpers render empty values booleans lists and titles` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:239`
  - fixture/arg: なし
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-136

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_load_analysis_reports_missing_work_missing_file_and_non_object
```

- 確認内容: pytest case `load analysis reports missing work missing file and non object` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:255`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-137

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_default_analysis_ignores_non_string_assumptions_and_analysis_template_missing_work
```

- 確認内容: pytest case `default analysis ignores non string assumptions and analysis template missing work` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:270`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-138

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_require_github_operation_gate_reports_missing_contexts
```

- 確認内容: pytest case `require github operation gate reports missing contexts` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:292`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-139

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_require_github_operation_gate_rejects_unapproved_mutation_and_rag
```

- 確認内容: pytest case `require github operation gate rejects unapproved mutation and rag` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:300`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-140

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_build_repair_sync_and_rag_markdown_include_dynamic_sections
```

- 確認内容: pytest case `build repair sync and rag markdown include dynamic sections` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:311`
  - fixture/arg: なし
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-141

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_build_sync_plan_renders_empty_action_placeholder
```

- 確認内容: pytest case `build sync plan renders empty action placeholder` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:330`
  - fixture/arg: なし
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-142

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_history_rewrite_candidate_validation_edges
```

- 確認内容: pytest case `history rewrite candidate validation edges` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:337`
  - fixture/arg: なし
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-143

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_detect_history_rewrite_candidates_from_commit_log
```

- 確認内容: pytest case `detect history rewrite candidates from commit log` はcommit漏れ検出が1-3 file候補をpendingで算出することを確認する。
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:378`
  - fixture/arg: `monkeypatch` (environment / function monkeypatch), `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: `calls`
- 期待結果: pytest case pass。検出候補は `HISTORY-DETECT-001`、`approval_status: pending`、吸収先commitを含む。

#### RT-UT-CASE-143A

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_create_detect_rebase_candidates_writes_analysis
```

- 確認内容: pytest case `create detect rebase candidates writes analysis` は検出結果がanalysis JSONへ書き戻されることを確認する。
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:418`
  - fixture/arg: `monkeypatch` (environment / function monkeypatch), `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: `detected`, `updated`
- 期待結果: pytest case pass。`history_rewrite_candidates` が検出結果で更新され、候補数が返る。

#### RT-UT-CASE-143B

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_create_repair_plan_writes_output_and_registers_artifact
```

- 確認内容: pytest case `create repair plan writes output and registers artifact` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:444`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: `artifact_index`
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-144

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_create_rebase_plan_writes_output_and_registers_artifact
```

- 確認内容: pytest case `create rebase plan writes output and registers artifact` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:467`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: `artifact_index`
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-145

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_create_rebase_apply_requires_human_and_candidate_approval
```

- 確認内容: pytest case `create rebase apply requires human and candidate approval` はrebase実行がCLI承認と候補承認の両方を要求することを確認する。
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:503`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: pytest case pass。未承認は拒否され、承認済みdry-runでは計画済みcommandのみ返る。

#### RT-UT-CASE-145A

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_github_sync_command_validation_edges
```

- 確認内容: pytest case `github sync command validation edges` はGitHub sync applyが許可されたgh commandだけを受け付けることを確認する。
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:554`
  - fixture/arg: なし
  - parameter: names=なし, case=なし
  - inline input: `action`
- 期待結果: pytest case pass。shell chaining、gh以外、target不一致、広すぎるgh apiは拒否される。

#### RT-UT-CASE-145B

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_create_sync_apply_requires_approval_and_records_result
```

- 確認内容: pytest case `create sync apply requires approval and records result` はGitHub sync applyがHuman Checkとaction承認を要求し、結果をanalysis JSONへ記録することを確認する。
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:581`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: `updated`
- 期待結果: pytest case pass。未承認は拒否され、承認済みdry-runは `execution_status` と `execution_result` を保存する。

#### RT-UT-CASE-145C

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_create_sync_apply_blocks_unresolved_rebase_candidates
```

- 確認内容: pytest case `create sync apply blocks unresolved rebase candidates` は未解決rebase候補が残っている場合にGitHub sync applyが停止することを確認する。
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:639`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: pytest case pass。未解決 `HISTORY-1` が残る場合はGitHub sync applyを実行しない。

#### RT-UT-CASE-145D

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_create_rag_candidate_requires_human_approval_for_publish
```

- 確認内容: pytest case `create rag candidate requires human approval for publish` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:541`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-146

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_create_rag_candidate_writes_explicit_output_with_ready_gate
```

- 確認内容: pytest case `create rag candidate writes explicit output with ready gate` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:560`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-147

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_create_rag_candidate_default_and_publish_outputs
```

- 確認内容: pytest case `create rag candidate default and publish outputs` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:584`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-148

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_run_dispatches_commands_and_rejects_unknown
```

- 確認内容: pytest case `run dispatches commands and rejects unknown` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:620`
  - fixture/arg: `monkeypatch` (environment / function monkeypatch)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????

#### RT-UT-CASE-149

- pytest node id:

```text
runtime/tests/test_github_knowledge_maintenance.py::test_main_prints_json
```

- 確認内容: pytest case `main prints json` ?????runtime?????????????error boundary???????
- 入力値:
  - pytest node: 上記コードブロックのnode id
  - source: `runtime/tests/test_github_knowledge_maintenance.py:654`
  - fixture/arg: `monkeypatch` (environment / function monkeypatch), `capsys` (captured stdout/stderr)
  - parameter: names=なし, case=なし
  - inline input: test関数内で生成される固定入力、mock入力、または一時ファイル
- 期待結果: ??case?pass????runtime????????????????????????
