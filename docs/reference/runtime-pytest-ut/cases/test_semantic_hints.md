# test_semantic_hints.py

This file records pytest node ids for `runtime/tests/test_semantic_hints.py`.

| Item | Value |
| --- | ---: |
| cases | 4 |

## Cases

#### RT-UT-CASE-AUTO-001

- pytest node id:

```text
runtime/tests/test_semantic_hints.py::test_semantic_hints_generate_writes_rag_source_markdown
```

- Confirm: `test_semantic_hints_generate_writes_rag_source_markdown` runtime contract is covered by pytest assertions.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_semantic_hints.py:39`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=none case=none
  - inline input: `text`
- Expected: pytest assertion defines the expected result.

#### RT-UT-CASE-AUTO-002

- pytest node id:

```text
runtime/tests/test_semantic_hints.py::test_semantic_hints_read_filters_backup_entries
```

- Confirm: `test_semantic_hints_read_filters_backup_entries` runtime contract is covered by pytest assertions.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_semantic_hints.py:63`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=none case=none
  - inline input: test-local fixtures and assertions
- Expected: pytest assertion defines the expected result.

#### RT-UT-CASE-AUTO-003

- pytest node id:

```text
runtime/tests/test_semantic_hints.py::test_semantic_hints_build_generates_then_runs_rag_build
```

- Confirm: `test_semantic_hints_build_generates_then_runs_rag_build` runtime contract is covered by pytest assertions.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_semantic_hints.py:82`
  - fixture/arg: `monkeypatch` (environment / function monkeypatch), `tmp_path` (temporary filesystem)
  - parameter: names=none case=none
  - inline input: `calls`
- Expected: pytest assertion defines the expected result.

#### RT-UT-CASE-AUTO-004

- pytest node id:

```text
runtime/tests/test_semantic_hints.py::test_normalize_documents_preserves_semantic_hint
```

- Confirm: `test_normalize_documents_preserves_semantic_hint` runtime contract is covered by pytest assertions.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_semantic_hints.py:132`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=none case=none
  - inline input: `normalized`
- Expected: pytest assertion defines the expected result.
