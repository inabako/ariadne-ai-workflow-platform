# test_runtime_log.py

This file records pytest node ids for `runtime/tests/test_runtime_log.py`.

| Item | Value |
| --- | ---: |
| cases | 10 |

## Cases

#### RT-UT-CASE-AUTO-001

- pytest node id:

```text
runtime/tests/test_runtime_log.py::test_runtime_log_summary_counts_events_traces_and_commands
```

- Confirm: runtime log summary counts lines, parsed events, traces, event kinds, and top commands.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_runtime_log.py`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=none case=none
  - inline input: six runtime event log lines generated by `RuntimeEventLogger`
- Expected: summary returns `runtime-log-summary`, parsed counts, and read-only next action.

#### RT-UT-CASE-AUTO-002

- pytest node id:

```text
runtime/tests/test_runtime_log.py::test_runtime_log_prune_dry_run_reports_counts_without_writing
```

- Confirm: prune dry-run reports removal and keep counts without rewriting the runtime log.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_runtime_log.py`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: keep_last=2
  - inline input: six runtime event log lines
- Expected: status is `dry-run`, four lines are planned for prune, and the log content remains unchanged.

#### RT-UT-CASE-AUTO-003

- pytest node id:

```text
runtime/tests/test_runtime_log.py::test_runtime_log_prune_requires_human_check_before_writing
```

- Confirm: non-dry-run prune requires explicit human approval before writing.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_runtime_log.py`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: keep_last=2, human_check=none
  - inline input: six runtime event log lines
- Expected: status is `human-check-required`, `would_write` is false, and the log content remains unchanged.

#### RT-UT-CASE-AUTO-004

- pytest node id:

```text
runtime/tests/test_runtime_log.py::test_runtime_log_archive_with_approval_writes_archive_and_keeps_tail
```

- Confirm: approved archive writes an archive file and keeps only the requested tail in the active runtime log.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_runtime_log.py`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: keep_last=2, human_check=approved
  - inline input: six runtime event log lines
- Expected: four lines are archived, two lines remain in the active log, and one archive file is created.

#### RT-UT-CASE-AUTO-005

- pytest node id:

```text
runtime/tests/test_runtime_log.py::test_runtime_log_tail_filters_problem_events
```

- Confirm: `aiwfctl log tail --problems` style filtering can show only recent blocked, failed, warning, or error events.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_runtime_log.py`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: limit=1, problems=true
  - inline input: normal runtime events plus one blocked preflight event
- Expected: tail result returns one blocked problem event from `trace-problem`.

#### RT-UT-CASE-AUTO-006

- pytest node id:

```text
runtime/tests/test_runtime_log.py::test_runtime_log_grep_filters_trace_id
```

- Confirm: runtime log grep extracts events for one trace id.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_runtime_log.py`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: trace_id=trace-two
  - inline input: runtime event logs for two trace ids
- Expected: grep result includes only `trace-two` events.

#### RT-UT-CASE-AUTO-007

- pytest node id:

```text
runtime/tests/test_runtime_log.py::test_ctl_log_export_writes_trace_evidence
```

- Confirm: `aiwfctl log export --trace-id --output` writes trace log evidence through the official CTL entrypoint.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_runtime_log.py`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: CLI args for `log export --trace-id trace-export --output work/evidence/trace-export.json --json`
  - inline input: repository-local runtime event log fixture
- Expected: export result and saved JSON include `runtime-log-export` and the selected trace events.

#### RT-UT-CASE-AUTO-001

- pytest node id:

```text
runtime/tests/test_runtime_log.py::test_ctl_log_acknowledge_problem_hides_it_from_status
```

- Confirm: `test_ctl_log_acknowledge_problem_hides_it_from_status` runtime contract is covered by pytest assertions.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_runtime_log.py:152`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=none case=none
  - inline input: `args`, `result`
- Expected: pytest assertion defines the expected result.

#### RT-UT-CASE-AUTO-002

- pytest node id:

```text
runtime/tests/test_runtime_log.py::test_runtime_log_acknowledge_all_matching_command
```

- Confirm: `test_runtime_log_acknowledge_all_matching_command` runtime contract is covered by pytest assertions.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_runtime_log.py:192`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: names=none case=none
  - inline input: test-local fixtures and assertions
- Expected: pytest assertion defines the expected result.

#### RT-UT-CASE-AUTO-008

- pytest node id:

```text
runtime/tests/test_runtime_log.py::test_ctl_log_summary_and_prune_dry_run_route
```

- Confirm: `aiwfctl log summary` and `aiwfctl log prune --dry-run --output` are exposed through the official CTL entrypoint.
- Input:
  - pytest node: above node id
  - source: `runtime/tests/test_runtime_log.py`
  - fixture/arg: `tmp_path` (temporary filesystem)
  - parameter: CLI args for `log summary --json` and `log prune --keep-last 2 --dry-run --output work/evidence/log-prune-dry-run.json`
  - inline input: repository-local runtime event log fixture
- Expected: CTL summary returns JSON and dry-run prune does not shrink the log while saving the preview JSON.
