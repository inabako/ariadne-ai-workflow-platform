# Operation Guide

Use `scripts/list-servers.sh` for a smoke check and `scripts/validate.sh` for template tests.

Configure real servers in `config/servers.example.yaml` copied to an environment-specific config file.

