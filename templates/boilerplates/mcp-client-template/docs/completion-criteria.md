# Completion Criteria

The initial boilerplate is complete when:

- Multiple server descriptors can be registered.
- A server can be connected and disconnected.
- Capabilities are cached only after connection.
- Prompt, resource, and tool operations require an active session.
- Resource URI policy rejects local path interpretation.
- Tests pass using a mock MCP server.

