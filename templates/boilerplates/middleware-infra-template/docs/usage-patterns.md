# Usage Patterns

Supported initial Redis usage patterns:

- cache
- session
- temporary state
- distributed lock helper
- rate limiting
- Pub/Sub
- queue helper

Use Human Check when strict delivery, durable queue semantics, or consensus-grade locking is required.

