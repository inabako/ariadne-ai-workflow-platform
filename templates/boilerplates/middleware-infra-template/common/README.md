# Common Contracts

Common middleware contracts are shared across environments and Redis profiles.

Do not store generated secrets, private keys, or runtime evidence containing sensitive values here.

