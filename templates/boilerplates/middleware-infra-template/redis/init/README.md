# Redis Init

Place optional initialization scripts here only when the requirement document explicitly needs seed keys or runtime preparation.

Do not store real secrets, production data, or personal data in this directory.

