# Notification Design

Runtime events are converted into concise Discord messages:

- `job.started`
- `job.progress`
- `job.waiting_for_input`
- `job.completed`
- `job.failed`

Mentions are escaped before delivery.

