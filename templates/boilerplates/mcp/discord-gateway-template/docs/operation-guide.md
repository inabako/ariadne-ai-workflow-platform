# Operation Guide

Use `scripts/run-gateway.sh` for a mock gateway smoke check and `scripts/validate.sh` for tests.

Wire a maintained Discord library adapter before connecting to a real Discord application.

