# Human Check UI

Human Check prompts are displayed with approve and reject actions. The component token does not contain raw job IDs, user IDs, or secrets.

The Gateway submits the response to Agent Runtime. Runtime remains the source of truth for approval state.

