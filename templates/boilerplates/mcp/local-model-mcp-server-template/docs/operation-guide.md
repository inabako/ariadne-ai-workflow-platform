# Operation Guide

Use `scripts/run-stdio.sh` for local stdio smoke checks and `scripts/validate.sh` for template tests.

Streamable HTTP is represented as a policy and extension point. Add the official MCP SDK transport adapter before exposing HTTP to other processes.

