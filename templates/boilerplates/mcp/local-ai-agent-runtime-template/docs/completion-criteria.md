# Completion Criteria

The initial boilerplate is complete when:

- Jobs can be submitted.
- Workflow steps run in order.
- Human check steps pause safely.
- Checkpoints are written as JSON.
- Completion requires all required steps and evidence.
- Tests pass without a real MCP server or model.

