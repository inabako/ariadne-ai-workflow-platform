# MySQL Configuration

Use `docker-compose/env.example` as a placeholder-only sample. Real passwords must come from an approved secret source.

Set database name, timezone, max connections, volume name, backup target, and restore validation in the copied project.

