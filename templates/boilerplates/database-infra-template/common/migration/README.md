# Database Migration Contract

Migration tooling is project-defined. The boilerplate provides entrypoints and evidence rules without forcing Flyway, Liquibase, Prisma, Drizzle, Alembic, or a custom SQL runner.

