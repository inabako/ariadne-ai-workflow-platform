# Authorization Policy

Authorization checks:

- Guild is allowed.
- Channel is allowed.
- At least one required role is present when role allow-list is configured.
- Direct messages are denied by default.

The template stores example allow-lists only. Real Discord IDs belong in environment-specific config.

