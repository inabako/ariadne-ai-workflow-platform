# Completion Criteria

The initial boilerplate is complete when:

- Commands map to Runtime DTOs.
- Authorization denies disallowed subjects.
- Rate limit blocks requests before Runtime calls.
- Human Check tokens validate and reject tampering.
- Runtime events are deduplicated and formatted for Discord.
- Tests pass without real Discord or Runtime services.

