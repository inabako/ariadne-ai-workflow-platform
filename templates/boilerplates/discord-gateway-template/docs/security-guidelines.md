# Security Guidelines

- Store bot tokens in environment variables, never in source.
- Deny direct messages by default.
- Escape Discord mentions.
- Do not download attachments into Runtime workspace by default.
- Do not expose stack traces to Discord.
- Do not call MCP tools directly from Discord commands.

