# Common Platform Infrastructure

`common/` contains shared contracts only. Product-specific setup belongs under `gitlab/`, `jenkins/`, `grafana/`, or `zabbix/`.

Allowed shared responsibilities:

- network
- storage
- secrets contract
- certificates contract
- backup contract
- health check contract
- evidence collection
- rollback contract
- validation contract

