# Database Network Contract

- Default to internal network only.
- External exposure must be explicit and human-approved.
- Record port, source CIDR, TLS mode, and health check route.
- Avoid host port conflicts across PostgreSQL and MySQL.

