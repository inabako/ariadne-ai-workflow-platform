# Database Evidence Contract

Evidence should include engine, version, deployment mode, startup result, connectivity, initialization, read / write, persistence, backup, restore, migration, logs, timestamp, and pass / fail decision.

Do not include passwords, secret values, or complete connection strings.

