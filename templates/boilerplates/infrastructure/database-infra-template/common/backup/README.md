# Database Backup Contract

Backup completion requires file creation, nonzero size, timestamp, source DB, and retention policy evidence.

Backup success alone is not enough; restore validation is a separate required check when enabled.

