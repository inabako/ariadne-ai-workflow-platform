# Security Tests

- Application user cannot perform administrator-only operations.
- Unnecessary external exposure is absent.
- Secrets are not logged.
- Default passwords are not used outside local placeholder validation.

