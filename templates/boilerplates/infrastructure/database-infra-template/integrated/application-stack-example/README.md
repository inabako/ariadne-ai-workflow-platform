# Application Stack Example

This profile shows how an application boilerplate receives a database connection contract without receiving database admin credentials.

The application uses secret references and app-user credentials only.

