apiVersion: v1
kind: Service
metadata:
  name: {{APP_NAME}}
  namespace: {{NAMESPACE}}
  labels:
    app.kubernetes.io/name: {{APP_NAME}}
spec:
  type: {{SERVICE_TYPE}}
  selector:
    app.kubernetes.io/name: {{APP_NAME}}
  ports:
    - name: http
      port: {{PORT}}
      targetPort: http
