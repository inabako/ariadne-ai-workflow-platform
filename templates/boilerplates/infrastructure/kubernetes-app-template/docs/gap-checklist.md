# Kubernetes / k3s Gap Checklist

- container image / tag
- Service port / targetPort
- readiness / liveness probe
- resources requests / limits
- Secret / ConfigMap boundary
- exposure policy
- storage policy for stateful workloads
- k3s local cluster constraints
