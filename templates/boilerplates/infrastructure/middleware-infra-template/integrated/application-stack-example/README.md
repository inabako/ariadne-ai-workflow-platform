# Application Stack Example

This example shows an application consuming Redis through the connection contract.

The application service is a placeholder. Replace it in the copied target repository only.

