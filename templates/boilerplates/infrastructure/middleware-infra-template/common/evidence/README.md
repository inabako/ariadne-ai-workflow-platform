# Evidence Contract

Redis evidence records requirements, version, purpose, memory policy, persistence, connectivity, read/write, TTL, restart, backup/restore, security checks, logs, and final result.

Do not include passwords, secret values, private keys, or production data.

