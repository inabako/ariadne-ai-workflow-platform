# Custom Component Development

Custom Collector components are outside the default template path.

Use a separate issue and Human Check for custom receiver, processor, exporter, connector, or extension code.
