# Troubleshooting

Common checks:

- `make validate`
- `make inspect-distribution`
- `make generate`
- `terraform -chdir=terraform validate`
- Collector logs with the debug exporter enabled
- Health endpoint on port `13133`
