# Distribution Extension

Use distribution extension only when the selected component is unavailable in the selected distribution or when a custom lightweight binary is explicitly required.

Human Check is required before custom distribution build.
