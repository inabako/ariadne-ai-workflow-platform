# Distribution

Use the stock Collector distribution by default. Generate a custom distribution only when a required component is missing or an explicit lightweight binary is approved.

Human Check is required before running OCB for production-oriented work.

Default pinned versions:

- Collector: `0.156.0`
- Builder: `0.156.0`
- confmap providers: `1.62.0`
