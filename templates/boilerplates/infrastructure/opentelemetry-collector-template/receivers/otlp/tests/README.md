# OTLP Receiver Tests

Expected checks:

- Collector accepts the generated config.
- OTLP gRPC and HTTP ports are reachable in the target environment.
- Debug exporter receives at least one trace, metric, or log sample during smoke testing.
