schema_version: "1.0.0"
component:
  id: "otlp-receiver"
  collector_type: "receiver"
  collector_name: "otlp"
  display_name: "OTLP Receiver"
  description: "Receives traces, metrics, and logs over OTLP gRPC or HTTP."
source:
  repository: "opentelemetry-collector"
  go_module: "go.opentelemetry.io/collector/receiver/otlpreceiver"
  module_version: "v0.156.0"
distribution:
  required: "core"
  custom_build_required: false
signals:
  traces:
    supported: true
    stability: "stable"
  metrics:
    supported: true
    stability: "stable"
  logs:
    supported: true
    stability: "stable"
dependencies:
  components: []
  platforms: []
conflicts:
  components: []
files:
  config_fragment: "config.yaml"
  test_config: "test-config.yaml"
  documentation: "README.md"
  tests: []
pipeline:
  placement: "receiver"
  compatible_pipelines:
    - "traces"
    - "metrics"
    - "logs"
validation:
  required_commands:
    - "collector-config-validation"
    - "smoke-test"
security:
  network_exposure: true
  secret_fields: []
review:
  human_gate_required: false
