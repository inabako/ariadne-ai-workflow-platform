# Web Application Example

This example shows a web application consuming OpenLDAP through the identity connection contract.

The application service is a placeholder. Replace it in the copied target repository only.

