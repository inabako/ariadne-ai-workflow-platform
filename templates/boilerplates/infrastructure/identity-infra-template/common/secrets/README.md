# Secrets Contract

Only secret references are stored in this template.

Required references:

- `ldap.admin.bind_dn`
- `ldap.admin.password`
- `ldap.application.bind_dn`
- `ldap.application.password`

Do not store actual passwords, password hashes, complete credentials, or private keys.

