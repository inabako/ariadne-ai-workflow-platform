# TLS Guidelines

TLS decisions must define:

- LDAP, LDAPS, or StartTLS mode
- certificate source
- certificate verification behavior
- local self-signed certificate limitation
- production certificate rotation owner

Plain LDAP for production-like credentials requires Human Check.

