# Normalized RAG Documents

このディレクトリには、source Markdown report を RAG 用 JSON document に変換したファイルを保存します。

生成元:

```text
runtime/rag/normalize_documents.py
```

出力例:

```text
rag/normalized/<uuid>.json
```
